<UserControl x:Class="ArcGISRuntime.WPF.Viewer.CodeListing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArcGISRuntime.WPF.Viewer"
             xmlns:converters="clr-namespace:ArcGISRuntime.WPF.Viewer.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:SampleToBitmapConverter x:Key="sampleToBitmapConverter" />
        <converters:NullToVisibilityConverter x:Key="nullToVisibilityConverter" />
    </UserControl.Resources>

        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <ComboBox IsEditable="False" ItemsSource="{Binding CodeFileNames}" SelectionChanged="ListBox_SelectionChanged" x:Name="lstCodeFiles" Grid.Row="0"/>
            <WebBrowser x:Name="txtCodeListing" Grid.Row="1" />
        </Grid>
</UserControl>
